let address = require('./address')
let location = require('./locations')
module.exports = {
    id: 'string',
    name: 'string',
    code: 'string',
    shortName: 'string',
    type: 'string',
    location: location,
    address: address
}
