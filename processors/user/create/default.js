'use strict'

exports.process = async (user, context) => {
    // let to = {}
    // let modes = []

    // if (!user.phone) {
    //     to = { 'email': user.email }
    //     modes.push('email')
    // } else {
    //     to = { 'phone': user.phone }
    //     modes.push('sms')
    // }

    // context.logger.debug(`sending message to ${to}`)
    // await communications.send({ otp: user.otp },
    //     context.tenant.code + '-otp', [to],
    //     context.tenant.owner.key,
    //     modes, { isHidden: true }
    // )
    // context.logger.debug('otp delivered')
}
